import { StyleSheet, Dimensions } from "react-native"

export default StyleSheet.create({
    modal: {
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
    },
    body: {
        flex: 1,
        alignItems: "center",
        justifyContent: "center",
        flexGrow: 0.3,
    },
})
